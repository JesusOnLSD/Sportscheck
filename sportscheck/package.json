{
  "name": "sportscheck-backend",
  "version": "1.0.0",
  "description": "Backend for Sportscheck — spawns the Flashscore scraper as a Python subprocess, stores results in SQLite, serves them via Express, and generates AI analysis and news summaries on demand via the Claude API.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
